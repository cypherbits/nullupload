<?php

$action = isset($_POST['f']) ? trim($_POST['f']) : null;


if (!empty($action)){
    switch ($action){
        case 'used':
            break;
        case 'fileExists':
            break;
    }
}
